<footer class="footer">
<% if user_signed_in? %> 

<% @post = Post.new unless @post %>

<html lang="ja">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>

    <button class="btn btn-primary" data-toggle="modal" data-target="#modal-postform" data-backdrop="true">
        画像投稿
    </button>
    <div class="modal fade" id="modal-postform" tabindex="-1">
      <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">
                    <span aria-hidden="true">&times;</span>
                </button>
                <h4 class="modal-title" id="modal-label">投稿</h4>
            </div>
            <div class="modal-body">
                <section class="post_form">
                <%= render 'shared/post_form' %>
                </section>
            </div>
      </div>
    </div>
</body>
</html>
<% else %>
<% end %>

  <nav>
    <ul>
      <li><%= link_to "About",   about_path %></li>
      <li><%= link_to "Contact", contact_path %></li>
      <li><a href="https://www.instagram.com/?hl=ja">Original</a></li>
    </ul>
  </nav>
</footer>
